name: CI - Fintech Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          
      - name: Instalar dependências
        run: |
          pip install pytest pyspark==3.4.0 flake8

      - name: Linting com flake8
        run: |
          flake8 tests/test_transformations.py --max-line-length=100 --ignore=E501,W503

      - name: Rodar testes unitários
        run: |
          pytest tests/test_transformations.py -v --tb=short
